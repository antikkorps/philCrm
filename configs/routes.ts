const DASHBOARD_BASE = "/dashboard"
export const ROUTES = {
    register: "/register",
    login: "/login",
    dashboard: {
        index: DASHBOARD_BASE,
        settings: `${DASHBOARD_BASE}/settings`,
    },
}